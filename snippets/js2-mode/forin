# -*- mode: snippet -*-
# name: for in
# key: forin
# --
for (${1:$$(yas-choose-value '("const" "let"))} ${2:variable} of ${3:iterable}) {
    ${3:if (${3:object}.hasOwnProperty(${2:variable})) {
        $4
    }}
}