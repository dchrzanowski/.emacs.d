# -*- mode: snippet -*-
# name: StatelessWidget
# --
class $1 extends StatelessWidget {
  const $1({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      $2
    );
  }
}